{
  "name": "mqtt",
  "version": "1.0.0",
  "description": "console example how to communicate with mqtt server and store data into mysql database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^4.2.6"
  }
}
